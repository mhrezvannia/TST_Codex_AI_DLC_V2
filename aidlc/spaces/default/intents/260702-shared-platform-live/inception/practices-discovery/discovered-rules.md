# Discovered Rules - Shared Platform Local Functionality

## Mandated

<!-- No new project-level ALWAYS rules are proposed by this freshness run. Existing affirmed project rules already cover trunk-based delivery, squash-merged Bolts, gated walking skeleton, test gates, backend coverage, Enterprise Technical Environment alignment, self-hosted staging deployment, and manual production approval. -->

## Forbidden

<!-- No new project-level NEVER rules are proposed by this freshness run. Existing affirmed project rules already forbid long-lived release branches, skipping the walking skeleton, treating downstream modules as part of this Shared Platform workflow, polluting backend domain-core, and using prohibited frontend tooling. -->
