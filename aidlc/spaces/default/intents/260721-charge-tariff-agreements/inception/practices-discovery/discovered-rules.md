# Discovered Rules — W2-03 Charge Tariffs & Agreements

## Mandated

ALWAYS keep W2-03 on its Wave A intent branch and integrate through `integ/main-reconciled` according to the program backlog dependency and merge protocol.

ALWAYS make the first W2-03 Construction slice prove one real approved Charge rate line stored and rendered by Booking before expanding the pricing journey.

ALWAYS write tests alongside W2-03 code, enforce at least 80 percent line coverage for changed Charge and Booking code, and add Charge lint/build to blocking quality gates.

ALWAYS require domain, additive-migration, provider-consumer contract, typed-snapshot compatibility, repricing, manual-state, Playwright, isolated Compose, demo-guard, `aidlc-audit`, and `erp-fidelity-audit` evidence before release completion.

ALWAYS preserve Charge pricing authority, Booking snapshot/UI ownership, service-owned databases, framework-free domain cores, session-derived actors, correlation/idempotency metadata, and typed failure semantics.

ALWAYS use `scripts/wave-a-compose.mjs` with the isolated `linercore-wave-a` project and run `npm run demo:guard` before and after live acceptance.

ALWAYS preserve fail-closed non-local profile/session/secret controls, immutable dependency installation, least-privilege workflow permissions, and explicit local-only credentials.

## Forbidden

NEVER call the hardcoded Charge workbench, static documents, unit tests, container startup, or unavailable Docker access a W2-03 live or release PASS.

NEVER flatten a successful W2-03 price into lossy string-only lines or represent no-rate as zero, guessed, partial, or transient-failure pricing.

NEVER redesign `packages/ui`, shared shell, navigation, typography, palette, or another domain's pages as part of Charge-owned W2-03 UI work.

NEVER query another service's database, bypass the existing Booking-to-Charge pricing port, or move pricing authority into a generic shared UI/service layer.

NEVER rewrite the W1 blocked/waived history as a real PASS or weaken W0-01, W0-02, W1-01, W2-01, or W2-02 evidence to simplify this feature.

NEVER describe the current CI as security-complete or expand W2-03 into repository-wide SAST, DAST, secret/CVE/image scanning, SBOM, signing, or deployment modernization without a separately approved scope change.

## Evidence Sources

Derived from `code-structure.md`, `technology-stack.md`, `dependencies.md`, `code-quality-assessment.md`, `architecture.md`, and `business-overview.md`, repository evidence, and the confirmed Practices Discovery answers.

