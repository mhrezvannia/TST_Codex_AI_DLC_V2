# Build and Test Results

## Canonical report

The engine-resolved results artifact is `build-test-results.md`. This
compatibility filename is retained because the stage prose historically named
`test-results.md`.

The report traces execution to each U01/U02/U03 `code-generation-plan.md` and
`code-summary.md`.

## Current outcome

See `build-test-results.md` for command-level pass, blocked, skipped, and
coverage evidence. No result in that report is upgraded from BLOCKED to PASS.
