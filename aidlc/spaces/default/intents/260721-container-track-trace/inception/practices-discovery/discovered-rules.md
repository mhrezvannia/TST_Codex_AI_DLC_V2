# Discovered Rules - W2-04

## Mandated

ALWAYS deliver W2-04 as vertical increments that retain domain, persistence, contract, Booking projection, UI, and observed evidence.
ALWAYS run the PB-01 broker-to-database-to-Booking walking skeleton before deeper lifecycle increments.
ALWAYS write risk-based tests alongside code for every legal transition and every duplicate or out-of-sequence rejection.
ALWAYS preserve service-owned databases, framework-free domain code, exact contract names, and producer/consumer co-review.
ALWAYS keep final Wave A live acceptance serialized, run the demo guard before and after, and retain evidence from any environmental retry.
ALWAYS preserve the historical W1 BLOCKED record and waiver separately from the later real PASS and new W2-04 evidence.

## Forbidden

NEVER treat a silent duplicate replay, timestamp-only ordering, outbox row, container startup, or screenshot as W2-04 acceptance.
NEVER run ordinary pull-request CI against the single shared Wave A live stack.
NEVER introduce a universal Java formatter or result-type migration as incidental W2-04 scope.
NEVER redesign `packages/ui`, the shared shell, or Booking while adding Container Movement-owned composition.
NEVER relabel an environmental block or accepted waiver as PASS.

## Evidence sources

Rules derive from `code-structure.md`, `technology-stack.md`,
`dependencies.md`, `code-quality-assessment.md`, `architecture.md`, and
`business-overview.md`, plus the approved W2-04 intent constraints.
