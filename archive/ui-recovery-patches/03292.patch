*** Begin Patch
*** Update File: D:\TST_Codex_ui_recovery\compose.yaml
@@
     volumes:
       - ./infrastructure/keycloak/linercore-local-realm.json:/opt/keycloak/data/import/linercore-local-realm.json:ro
+      - ./infrastructure/keycloak/themes:/opt/keycloak/themes:ro
*** Update File: D:\TST_Codex_ui_recovery\infrastructure\keycloak\linercore-local-realm.json
@@
   "realm": "linercore-local",
+  "displayName": "LinerCore",
   "enabled": true,
+  "loginTheme": "linercore",
   "sslRequired": "none",
*** Add File: D:\TST_Codex_ui_recovery\infrastructure\keycloak\themes\linercore\login\theme.properties
+parent=keycloak
+import=common/keycloak
+
+styles=css/login.css css/linercore-login.css
+scripts=js/linercore-login.js
+meta=viewport==width=device-width,initial-scale=1
+
+environmentLabel=LOCAL DEMO
*** Add File: D:\TST_Codex_ui_recovery\infrastructure\keycloak\themes\linercore\login\messages\messages_en.properties
+loginTitle=Sign in to LinerCore
+loginTitleHtml=LinerCore
+loginAccountTitle=Sign in
+loginInstruction=Use your company account to continue.
+doLogIn=Sign in
+signingIn=Signing in...
+skipToSignIn=Skip to sign in
+capsLockWarning=Caps Lock is on
+authorizedUsersOnly=Authorized users only
+accessSupport=Contact your platform administrator for access support.
+invalidUserMessage=Sign-in failed. Check your username or password and try again.
+invalidUsernameMessage=Sign-in failed. Check your username or password and try again.
+invalidPasswordMessage=Sign-in failed. Check your username or password and try again.
+accountDisabledMessage=This account cannot sign in. Contact your platform administrator.
+accountTemporarilyDisabledMessage=This account cannot sign in right now. Contact your platform administrator or try again later.
+expiredCodeMessage=Your session expired. Sign in again to continue.
+identityProviderAuthenticationFailedMessage=The identity provider is unavailable. Try again or contact your platform administrator.
+identityProviderUnexpectedErrorMessage=The identity provider is unavailable. Try again or contact your platform administrator.
+updatePasswordTitle=Update your password
+updatePasswordMessage=Create a new password to continue to LinerCore.
*** Add File: D:\TST_Codex_ui_recovery\infrastructure\keycloak\themes\linercore\login\template.ftl
+<#macro registrationLayout bodyClass="" displayInfo=false displayMessage=true displayRequiredFields=false>
+<!DOCTYPE html>
+<html class="${properties.kcHtmlClass!}" lang="${locale.currentLanguageTag!'en'}">
+<head>
+    <meta charset="utf-8">
+    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
+    <meta name="robots" content="noindex, nofollow">
+
+    <#if properties.meta?has_content>
+        <#list properties.meta?split(' ') as meta>
+            <meta name="${meta?split('==')[0]}" content="${meta?split('==')[1]}">
+        </#list>
+    </#if>
+
+    <title>${msg("loginTitle", (realm.displayName!''))}</title>
+    <link rel="icon" href="${url.resourcesPath}/img/favicon.ico">
+
+    <#if properties.stylesCommon?has_content>
+        <#list properties.stylesCommon?split(' ') as style>
+            <link href="${url.resourcesCommonPath}/${style}" rel="stylesheet">
+        </#list>
+    </#if>
+    <#if properties.styles?has_content>
+        <#list properties.styles?split(' ') as style>
+            <link href="${url.resourcesPath}/${style}" rel="stylesheet">
+        </#list>
+    </#if>
+    <#if properties.scripts?has_content>
+        <#list properties.scripts?split(' ') as script>
+            <script src="${url.resourcesPath}/${script}" defer></script>
+        </#list>
+    </#if>
+    <script src="${url.resourcesPath}/js/menu-button-links.js" type="module"></script>
+
+    <#if scripts??>
+        <#list scripts as script>
+            <script src="${script}"></script>
+        </#list>
+    </#if>
+    <#if authenticationSession??>
+        <script type="module">
+            import { checkCookiesAndSetTimer } from "${url.resourcesPath}/js/authChecker.js";
+            checkCookiesAndSetTimer(
+                "${authenticationSession.authSessionId}",
+                "${authenticationSession.tabId}",
+                "${url.ssoLoginInOtherTabsUrl?no_esc}"
+            );
+        </script>
+    </#if>
+</head>
+
+<body class="${properties.kcBodyClass!} ${bodyClass}">
+<a class="linercore-skip-link" href="#kc-content">${msg("skipToSignIn")}</a>
+
+<main class="${properties.kcLoginClass!}">
+    <div id="kc-header" class="${properties.kcHeaderClass!}">
+        <div class="linercore-brand-row">
+            <span class="linercore-wordmark">LinerCore</span>
+            <span class="linercore-environment">${properties.environmentLabel!'LOCAL DEMO'}</span>
+        </div>
+        <p class="linercore-product-context">Shipping operations platform</p>
+    </div>
+
+    <section class="${properties.kcFormCardClass!}" aria-labelledby="kc-page-title">
+        <header class="${properties.kcFormHeaderClass!}">
+            <#if realm.internationalizationEnabled && locale.supported?size gt 1>
+                <div class="${properties.kcLocaleMainClass!}" id="kc-locale">
+                    <div id="kc-locale-wrapper" class="${properties.kcLocaleWrapperClass!}">
+                        <div id="kc-locale-dropdown" class="menu-button-links ${properties.kcLocaleDropDownClass!}">
+                            <button id="kc-current-locale-link" aria-label="${msg("languages")}" aria-haspopup="true"
+                                    aria-expanded="false" aria-controls="language-switch1">${locale.current}</button>
+                            <ul role="menu" tabindex="-1" aria-labelledby="kc-current-locale-link"
+                                id="language-switch1" class="${properties.kcLocaleListClass!}">
+                                <#assign i = 1>
+                                <#list locale.supported as l>
+                                    <li class="${properties.kcLocaleListItemClass!}" role="none">
+                                        <a role="menuitem" id="language-${i}" class="${properties.kcLocaleItemClass!}"
+                                           href="${l.url}">${l.label}</a>
+                                    </li>
+                                    <#assign i++>
+                                </#list>
+                            </ul>
+                        </div>
+                    </div>
+                </div>
+            </#if>
+
+            <#if !(auth?has_content && auth.showUsername() && !auth.showResetCredentials())>
+                <#if displayRequiredFields>
+                    <p class="linercore-required-fields"><span aria-hidden="true">*</span> ${msg("requiredFields")}</p>
+                </#if>
+                <h1 id="kc-page-title"><#nested "header"></h1>
+            <#else>
+                <#nested "show-username">
+                <div id="kc-username" class="${properties.kcFormGroupClass!}">
+                    <span id="kc-attempted-username">${auth.attemptedUsername}</span>
+                    <a id="reset-login" href="${url.loginRestartFlowUrl}" aria-label="${msg("restartLoginTooltip")}">
+                        <i class="${properties.kcResetFlowIcon!}" aria-hidden="true"></i>
+                    </a>
+                </div>
+            </#if>
+        </header>
+
+        <div id="kc-content">
+            <div id="kc-content-wrapper">
+                <#if displayMessage && message?has_content && (message.type != 'warning' || !isAppInitiatedAction??)>
+                    <div class="alert-${message.type} ${properties.kcAlertClass!} pf-m-<#if message.type = 'error'>danger<#else>${message.type}</#if>"
+                         role="<#if message.type = 'error'>alert<#else>status</#if>"
+                         aria-live="<#if message.type = 'error'>assertive<#else>polite</#if>">
+                        <div class="pf-c-alert__icon" aria-hidden="true">
+                            <#if message.type = 'success'><span class="${properties.kcFeedbackSuccessIcon!}"></span></#if>
+                            <#if message.type = 'warning'><span class="${properties.kcFeedbackWarningIcon!}"></span></#if>
+                            <#if message.type = 'error'><span class="${properties.kcFeedbackErrorIcon!}"></span></#if>
+                            <#if message.type = 'info'><span class="${properties.kcFeedbackInfoIcon!}"></span></#if>
+                        </div>
+                        <span class="${properties.kcAlertTitleClass!}">${kcSanitize(message.summary)?no_esc}</span>
+                    </div>
+                </#if>
+
+                <#nested "form">
+
+                <#if auth?has_content && auth.showTryAnotherWayLink()>
+                    <form id="kc-select-try-another-way-form" action="${url.loginAction}" method="post">
+                        <div class="${properties.kcFormGroupClass!}">
+                            <input type="hidden" name="tryAnotherWay" value="on">
+                            <button class="linercore-text-button" type="submit">${msg("doTryAnotherWay")}</button>
+                        </div>
+                    </form>
+                </#if>
+
+                <#nested "socialProviders">
+
+                <#if displayInfo>
+                    <div id="kc-info" class="${properties.kcSignUpClass!}">
+                        <div id="kc-info-wrapper" class="${properties.kcInfoAreaWrapperClass!}">
+                            <#nested "info">
+                        </div>
+                    </div>
+                </#if>
+            </div>
+        </div>
+    </section>
+
+    <footer class="linercore-login-footer">
+        <span>${msg("authorizedUsersOnly")}</span>
+        <span aria-hidden="true">&middot;</span>
+        <span>${msg("accessSupport")}</span>
+    </footer>
+</main>
+</body>
+</html>
+</#macro>
*** Add File: D:\TST_Codex_ui_recovery\infrastructure\keycloak\themes\linercore\login\login.ftl
+<#import "template.ftl" as layout>
+<@layout.registrationLayout displayMessage=!messagesPerField.existsError('username','password')
+                            displayInfo=realm.password && realm.registrationAllowed && !registrationDisabled??; section>
+    <#if section = "header">
+        <span>${msg("loginAccountTitle")}</span>
+        <span class="linercore-login-instruction">${msg("loginInstruction")}</span>
+    <#elseif section = "form">
+        <#if realm.password>
+            <form id="kc-form-login" action="${url.loginAction}" method="post">
+                <#if !usernameHidden??>
+                    <div class="${properties.kcFormGroupClass!}">
+                        <label for="username" class="${properties.kcLabelClass!}">
+                            <#if !realm.loginWithEmailAllowed>
+                                ${msg("username")}
+                            <#elseif !realm.registrationEmailAsUsername>
+                                ${msg("usernameOrEmail")}
+                            <#else>
+                                ${msg("email")}
+                            </#if>
+                        </label>
+                        <input id="username" class="${properties.kcInputClass!}" name="username"
+                               value="${(login.username!'')}" type="text" autofocus autocomplete="username"
+                               autocapitalize="none" spellcheck="false"
+                               <#if messagesPerField.existsError('username','password')>
+                                   aria-invalid="true" aria-describedby="input-error"
+                               </#if>>
+                    </div>
+                </#if>
+
+                <div class="${properties.kcFormGroupClass!}">
+                    <label for="password" class="${properties.kcLabelClass!}">${msg("password")}</label>
+                    <div class="${properties.kcInputGroup!}">
+                        <input id="password" class="${properties.kcInputClass!}" name="password"
+                               type="password" autocomplete="current-password"
+                               <#if messagesPerField.existsError('username','password')>
+                                   aria-invalid="true" aria-describedby="input-error"
+                               </#if>>
+                        <button class="${properties.kcFormPasswordVisibilityButtonClass!}" type="button"
+                                aria-label="${msg("showPassword")}" aria-controls="password"
+                                data-password-toggle
+                                data-icon-show="${properties.kcFormPasswordVisibilityIconShow!}"
+                                data-icon-hide="${properties.kcFormPasswordVisibilityIconHide!}"
+                                data-label-show="${msg('showPassword')}"
+                                data-label-hide="${msg('hidePassword')}">
+                            <i class="${properties.kcFormPasswordVisibilityIconShow!}" aria-hidden="true"></i>
+                        </button>
+                    </div>
+                    <p id="linercore-caps-lock" class="linercore-caps-lock" role="status" aria-live="polite" hidden>
+                        ${msg("capsLockWarning")}
+                    </p>
+
+                    <#if messagesPerField.existsError('username','password')>
+                        <p id="input-error" class="${properties.kcInputErrorMessageClass!}" role="alert">
+                            ${kcSanitize(messagesPerField.getFirstError('username','password'))?no_esc}
+                        </p>
+                    </#if>
+                </div>
+
+                <#if realm.rememberMe || realm.resetPasswordAllowed>
+                    <div class="${properties.kcFormGroupClass!} ${properties.kcFormSettingClass!}">
+                        <#if realm.rememberMe && !usernameHidden??>
+                            <label class="linercore-checkbox">
+                                <input id="rememberMe" name="rememberMe" type="checkbox"
+                                       <#if login.rememberMe??>checked</#if>>
+                                <span>${msg("rememberMe")}</span>
+                            </label>
+                        </#if>
+                        <#if realm.resetPasswordAllowed>
+                            <a href="${url.loginResetCredentialsUrl}">${msg("doForgotPassword")}</a>
+                        </#if>
+                    </div>
+                </#if>
+
+                <div id="kc-form-buttons" class="${properties.kcFormGroupClass!}">
+                    <input type="hidden" id="id-hidden-input" name="credentialId"
+                           <#if auth.selectedCredential?has_content>value="${auth.selectedCredential}"</#if>>
+                    <button class="${properties.kcButtonClass!} ${properties.kcButtonPrimaryClass!}
+                                   ${properties.kcButtonBlockClass!} ${properties.kcButtonLargeClass!}"
+                            name="login" id="kc-login" type="submit"
+                            data-submitting-label="${msg('signingIn')}">
+                        <span class="linercore-button-label">${msg("doLogIn")}</span>
+                        <span class="linercore-button-spinner" aria-hidden="true"></span>
+                    </button>
+                </div>
+            </form>
+        </#if>
+        <script type="module" src="${url.resourcesPath}/js/passwordVisibility.js"></script>
+    <#elseif section = "info">
+        <#if realm.password && realm.registrationAllowed && !registrationDisabled??>
+            <div id="kc-registration-container">
+                <span>${msg("noAccount")} <a href="${url.registrationUrl}">${msg("doRegister")}</a></span>
+            </div>
+        </#if>
+    <#elseif section = "socialProviders">
+        <#if realm.password && social.providers??>
+            <div id="kc-social-providers" class="${properties.kcFormSocialAccountSectionClass!}">
+                <hr>
+                <h2>${msg("identity-provider-login-label")}</h2>
+                <ul class="${properties.kcFormSocialAccountListClass!}">
+                    <#list social.providers as provider>
+                        <li>
+                            <a id="social-${provider.alias}"
+                               class="${properties.kcFormSocialAccountListButtonClass!}"
+                               href="${provider.loginUrl}">
+                                <#if provider.iconClasses?has_content>
+                                    <i class="${properties.kcCommonLogoIdP!} ${provider.iconClasses!}" aria-hidden="true"></i>
+                                </#if>
+                                <span class="${properties.kcFormSocialAccountNameClass!}">${provider.displayName!}</span>
+                            </a>
+                        </li>
+                    </#list>
+                </ul>
+            </div>
+        </#if>
+    </#if>
+</@layout.registrationLayout>
*** Add File: D:\TST_Codex_ui_recovery\infrastructure\keycloak\themes\linercore\login\resources\js\linercore-login.js
+(() => {
+  const form = document.getElementById("kc-form-login");
+  const password = document.getElementById("password");
+  const capsLockWarning = document.getElementById("linercore-caps-lock");
+
+  if (password && capsLockWarning) {
+    const updateCapsLock = (event) => {
+      const capsLockOn = event.getModifierState?.("CapsLock") ?? false;
+      capsLockWarning.hidden = !capsLockOn;
+    };
+
+    password.addEventListener("keydown", updateCapsLock);
+    password.addEventListener("keyup", updateCapsLock);
+    password.addEventListener("blur", () => {
+      capsLockWarning.hidden = true;
+    });
+  }
+
+  if (form) {
+    form.addEventListener("submit", () => {
+      if (form.dataset.submitting === "true") {
+        return;
+      }
+
+      form.dataset.submitting = "true";
+      form.setAttribute("aria-busy", "true");
+
+      const submitButton = document.getElementById("kc-login");
+      const submitLabel = submitButton?.querySelector(".linercore-button-label");
+      if (submitButton instanceof HTMLButtonElement) {
+        submitButton.disabled = true;
+      }
+      if (submitLabel && submitButton?.dataset.submittingLabel) {
+        submitLabel.textContent = submitButton.dataset.submittingLabel;
+      }
+    });
+  }
+})();
*** Add File: D:\TST_Codex_ui_recovery\infrastructure\keycloak\themes\linercore\login\resources\css\linercore-login.css
+:root {
+  color-scheme: light;
+  --erp-font-sans: "IBM Plex Sans", Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
+  --erp-space-1: 4px;
+  --erp-space-2: 8px;
+  --erp-space-3: 12px;
+  --erp-space-4: 16px;
+  --erp-space-5: 20px;
+  --erp-space-6: 24px;
+  --erp-space-8: 32px;
+  --erp-radius-sm: 6px;
+  --erp-radius-md: 8px;
+  --erp-color-bg: #f4f7fb;
+  --erp-color-surface: #ffffff;
+  --erp-color-surface-2: #eef3f9;
+  --erp-color-border: #d7e2ef;
+  --erp-color-border-strong: #c2d0e0;
+  --erp-color-text: #102235;
+  --erp-color-text-muted: #5a6b7d;
+  --erp-color-primary: #11427a;
+  --erp-color-primary-hover: #0d3663;
+  --erp-color-on-primary: #ffffff;
+  --erp-color-danger: #b42318;
+  --erp-color-danger-bg: #fbe9e7;
+  --erp-color-warning: #8a5200;
+  --erp-color-warning-bg: #fbf0dc;
+  --erp-color-info: #2a68b0;
+  --erp-color-info-bg: #e7f0fb;
+  --erp-focus-ring: 0 0 0 3px rgba(47, 115, 196, 0.45);
+}
+
+* {
+  box-sizing: border-box;
+}
+
+html,
+body {
+  min-height: 100%;
+}
+
+body.login-pf {
+  margin: 0;
+  min-width: 320px;
+  background: var(--erp-color-bg);
+  color: var(--erp-color-text);
+  font-family: var(--erp-font-sans);
+  font-size: 14px;
+  line-height: 1.5;
+}
+
+body.login-pf,
+body.login-pf::before {
+  background-image: none;
+}
+
+button,
+input,
+select,
+textarea {
+  font: inherit;
+}
+
+a {
+  color: var(--erp-color-primary);
+  text-underline-offset: 3px;
+}
+
+a:hover {
+  color: var(--erp-color-primary-hover);
+}
+
+a:focus-visible,
+button:focus-visible,
+input:focus-visible,
+select:focus-visible {
+  outline: none;
+  box-shadow: var(--erp-focus-ring);
+}
+
+.linercore-skip-link {
+  position: fixed;
+  z-index: 10;
+  top: var(--erp-space-3);
+  left: var(--erp-space-3);
+  padding: var(--erp-space-2) var(--erp-space-3);
+  transform: translateY(-160%);
+  border-radius: var(--erp-radius-sm);
+  background: var(--erp-color-surface);
+  color: var(--erp-color-primary);
+  font-weight: 700;
+}
+
+.linercore-skip-link:focus {
+  transform: translateY(0);
+}
+
+.login-pf-page {
+  display: flex;
+  width: min(100%, 432px);
+  min-height: 100vh;
+  margin: 0 auto;
+  padding: clamp(24px, 9vh, 88px) 16px 24px;
+  flex-direction: column;
+  justify-content: flex-start;
+}
+
+.login-pf-page .login-pf-page-header {
+  margin: 0 0 var(--erp-space-5);
+  padding: 0;
+  text-align: left;
+}
+
+.linercore-brand-row {
+  display: flex;
+  min-width: 0;
+  align-items: center;
+  justify-content: space-between;
+  gap: var(--erp-space-3);
+}
+
+.linercore-wordmark {
+  color: var(--erp-color-text);
+  font-size: 24px;
+  font-weight: 750;
+  line-height: 1.2;
+}
+
+.linercore-environment {
+  flex: 0 0 auto;
+  padding: 4px 8px;
+  border: 1px solid var(--erp-color-border);
+  border-radius: var(--erp-radius-sm);
+  background: var(--erp-color-surface-2);
+  color: var(--erp-color-text-muted);
+  font-size: 11px;
+  font-weight: 700;
+  line-height: 1.3;
+}
+
+.linercore-product-context {
+  margin: var(--erp-space-1) 0 0;
+  color: var(--erp-color-text-muted);
+  font-size: 13px;
+}
+
+.login-pf-page .card-pf {
+  width: 100%;
+  max-width: none;
+  margin: 0;
+  padding: var(--erp-space-8);
+  border: 1px solid var(--erp-color-border);
+  border-radius: var(--erp-radius-md);
+  background: var(--erp-color-surface);
+  box-shadow: 0 10px 30px rgba(16, 34, 53, 0.05);
+}
+
+.login-pf-page .login-pf-header {
+  margin: 0 0 var(--erp-space-6);
+  padding: 0;
+}
+
+#kc-page-title {
+  display: grid;
+  gap: var(--erp-space-1);
+  margin: 0;
+  color: var(--erp-color-text);
+  font-size: 24px;
+  font-weight: 700;
+  line-height: 1.25;
+  letter-spacing: 0;
+}
+
+.linercore-login-instruction {
+  color: var(--erp-color-text-muted);
+  font-size: 14px;
+  font-weight: 400;
+}
+
+.linercore-required-fields {
+  margin: 0 0 var(--erp-space-2);
+  color: var(--erp-color-text-muted);
+  font-size: 12px;
+}
+
+#kc-content,
+#kc-content-wrapper {
+  width: 100%;
+}
+
+#kc-form-login {
+  display: grid;
+  gap: var(--erp-space-5);
+}
+
+#kc-form-login .form-group,
+#kc-form-buttons {
+  margin: 0;
+}
+
+.pf-c-form__label {
+  display: inline-block;
+  margin: 0 0 var(--erp-space-2);
+  color: var(--erp-color-text);
+  font-size: 13px;
+  font-weight: 650;
+}
+
+.pf-c-form-control {
+  width: 100%;
+  height: 44px;
+  padding: 0 var(--erp-space-3);
+  border: 1px solid var(--erp-color-border-strong);
+  border-radius: var(--erp-radius-sm);
+  background: var(--erp-color-surface);
+  color: var(--erp-color-text);
+  box-shadow: none;
+}
+
+.pf-c-form-control:hover {
+  border-color: #91a6bd;
+}
+
+.pf-c-form-control:focus {
+  border-color: var(--erp-color-primary);
+  outline: none;
+  box-shadow: var(--erp-focus-ring);
+}
+
+.pf-c-form-control[aria-invalid="true"] {
+  border-color: var(--erp-color-danger);
+}
+
+.pf-c-input-group {
+  display: grid;
+  grid-template-columns: minmax(0, 1fr) 44px;
+}
+
+.pf-c-input-group .pf-c-form-control {
+  border-radius: var(--erp-radius-sm) 0 0 var(--erp-radius-sm);
+}
+
+.pf-c-input-group .pf-c-button.pf-m-control {
+  display: grid;
+  width: 44px;
+  min-width: 44px;
+  height: 44px;
+  padding: 0;
+  place-items: center;
+  border: 1px solid var(--erp-color-border-strong);
+  border-left: 0;
+  border-radius: 0 var(--erp-radius-sm) var(--erp-radius-sm) 0;
+  background: var(--erp-color-surface-2);
+  color: var(--erp-color-text-muted);
+}
+
+.pf-c-input-group .pf-c-button.pf-m-control:hover {
+  background: #e4ebf4;
+  color: var(--erp-color-text);
+}
+
+.pf-c-button.pf-m-primary {
+  position: relative;
+  display: inline-flex;
+  width: 100%;
+  min-height: 44px;
+  align-items: center;
+  justify-content: center;
+  gap: var(--erp-space-2);
+  padding: 0 var(--erp-space-4);
+  border: 1px solid var(--erp-color-primary);
+  border-radius: var(--erp-radius-sm);
+  background: var(--erp-color-primary);
+  color: var(--erp-color-on-primary);
+  font-weight: 700;
+  line-height: 1;
+}
+
+.pf-c-button.pf-m-primary:hover {
+  border-color: var(--erp-color-primary-hover);
+  background: var(--erp-color-primary-hover);
+  color: var(--erp-color-on-primary);
+}
+
+.pf-c-button.pf-m-primary:disabled {
+  cursor: wait;
+  opacity: 0.78;
+}
+
+.linercore-button-spinner {
+  display: none;
+  width: 14px;
+  height: 14px;
+  border: 2px solid rgba(255, 255, 255, 0.45);
+  border-top-color: var(--erp-color-on-primary);
+  border-radius: 50%;
+  animation: linercore-spin 700ms linear infinite;
+}
+
+#kc-form-login[aria-busy="true"] .linercore-button-spinner {
+  display: inline-block;
+}
+
+.linercore-caps-lock {
+  margin: var(--erp-space-2) 0 0;
+  color: var(--erp-color-warning);
+  font-size: 12px;
+  font-weight: 650;
+}
+
+.linercore-caps-lock[hidden] {
+  display: none;
+}
+
+#input-error,
+.pf-c-form__helper-text.pf-m-error {
+  display: block;
+  margin: var(--erp-space-2) 0 0;
+  color: var(--erp-color-danger);
+  font-size: 13px;
+  font-weight: 600;
+}
+
+.pf-c-alert {
+  display: grid;
+  grid-template-columns: 18px minmax(0, 1fr);
+  gap: var(--erp-space-2);
+  margin: 0 0 var(--erp-space-5);
+  padding: var(--erp-space-3);
+  border: 1px solid var(--erp-color-border);
+  border-radius: var(--erp-radius-sm);
+  box-shadow: none;
+}
+
+.pf-c-alert.pf-m-danger {
+  border-color: #e7b4ae;
+  background: var(--erp-color-danger-bg);
+  color: var(--erp-color-danger);
+}
+
+.pf-c-alert.pf-m-info {
+  border-color: #b8cee7;
+  background: var(--erp-color-info-bg);
+  color: var(--erp-color-info);
+}
+
+.pf-c-alert.pf-m-warning {
+  border-color: #e6c993;
+  background: var(--erp-color-warning-bg);
+  color: var(--erp-color-warning);
+}
+
+.pf-c-alert__title {
+  font-size: 13px;
+  line-height: 1.45;
+}
+
+.login-pf-settings {
+  display: flex;
+  min-height: 24px;
+  align-items: center;
+  justify-content: space-between;
+  gap: var(--erp-space-3);
+}
+
+.linercore-checkbox {
+  display: inline-flex;
+  align-items: center;
+  gap: var(--erp-space-2);
+}
+
+.linercore-checkbox input {
+  width: 16px;
+  height: 16px;
+  accent-color: var(--erp-color-primary);
+}
+
+.linercore-text-button {
+  padding: 0;
+  border: 0;
+  background: transparent;
+  color: var(--erp-color-primary);
+  text-decoration: underline;
+  text-underline-offset: 3px;
+}
+
+#kc-social-providers {
+  margin-top: var(--erp-space-6);
+}
+
+#kc-social-providers h2 {
+  color: var(--erp-color-text-muted);
+  font-size: 13px;
+  font-weight: 650;
+}
+
+.linercore-login-footer {
+  display: flex;
+  margin-top: var(--erp-space-4);
+  flex-wrap: wrap;
+  justify-content: center;
+  gap: var(--erp-space-2);
+  color: var(--erp-color-text-muted);
+  font-size: 12px;
+  text-align: center;
+}
+
+#kc-username {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: var(--erp-space-3);
+  margin: 0 0 var(--erp-space-4);
+  color: var(--erp-color-text);
+  font-weight: 650;
+}
+
+#reset-login {
+  display: grid;
+  width: 44px;
+  height: 44px;
+  place-items: center;
+  border-radius: var(--erp-radius-sm);
+}
+
+@keyframes linercore-spin {
+  to {
+    transform: rotate(360deg);
+  }
+}
+
+@media (max-width: 480px) {
+  .login-pf-page {
+    width: 100%;
+    padding: 24px 12px 20px;
+  }
+
+  .login-pf-page .card-pf {
+    padding: var(--erp-space-6);
+  }
+
+  .login-pf-page .login-pf-page-header {
+    margin-bottom: var(--erp-space-4);
+  }
+
+  .linercore-login-footer {
+    display: grid;
+  }
+
+  .linercore-login-footer [aria-hidden="true"] {
+    display: none;
+  }
+}
+
+@media (max-height: 620px) {
+  .login-pf-page {
+    padding-top: 20px;
+  }
+}
+
+@media (prefers-reduced-motion: reduce) {
+  *,
+  *::before,
+  *::after {
+    scroll-behavior: auto !important;
+    transition-duration: 0.01ms !important;
+    animation-duration: 0.01ms !important;
+    animation-iteration-count: 1 !important;
+  }
+}
*** End Patch
