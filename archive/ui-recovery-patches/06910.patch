*** Begin Patch
*** Update File: D:\TST_Codex_ui_recovery\packages\ui\src\interactive.tsx
@@
   useState,
+  type FocusEvent,
   type KeyboardEvent,
   type ReactNode
 } from "react";
+import { LucideIcon } from "./icons";
@@
 export function Combobox({
   options,
   value,
   onChange,
+  onBlur,
+  id,
+  name,
   placeholder,
   emptyLabel = "No matches",
+  disabled,
+  invalid,
+  describedBy,
+  required,
   "aria-label": ariaLabel
 }: {
   options: ComboOption[];
   value: string;
   onChange: (value: string) => void;
+  onBlur?: (event: FocusEvent<HTMLInputElement>) => void;
+  id?: string;
+  name?: string;
   placeholder?: string;
   emptyLabel?: string;
+  disabled?: boolean;
+  invalid?: boolean;
+  describedBy?: string;
+  required?: boolean;
   "aria-label"?: string;
 }) {
@@
   function onKeyDown(event: KeyboardEvent<HTMLInputElement>) {
+    if (disabled) return;
     if (event.key === "ArrowDown") {
@@
       <input
+        id={id}
+        name={name}
         className="erp-input"
         role="combobox"
         aria-expanded={open}
         aria-controls={listId}
         aria-autocomplete="list"
         aria-label={ariaLabel}
+        aria-invalid={invalid || undefined}
+        aria-describedby={describedBy}
+        aria-activedescendant={open && filtered[activeIndex] ? `${listId}-${activeIndex}` : undefined}
         placeholder={placeholder}
+        disabled={disabled}
+        required={required}
         value={open ? query : selectedLabel}
         onChange={(event) => { setQuery(event.target.value); setOpen(true); setActiveIndex(0); }}
         onFocus={() => setOpen(true)}
+        onBlur={(event) => {
+          window.setTimeout(() => setOpen(false), 0);
+          onBlur?.(event);
+        }}
         onKeyDown={onKeyDown}
       />
@@
               <li
                 key={option.value}
+                id={`${listId}-${index}`}
                 role="option"
@@
 export function Toasts({ toasts, onDismiss }: { toasts: ToastItem[]; onDismiss: (id: string) => void }) {
@@
-          <button type="button" className="erp-toast__close" aria-label="Dismiss notification" onClick={() => onDismiss(toast.id)}>Ãƒâ€”</button>
+          <button type="button" className="erp-toast__close" aria-label="Dismiss notification" onClick={() => onDismiss(toast.id)}>
+            <LucideIcon name="x" size={16} />
+          </button>
@@
 export function ThemeToggle() {
@@
   return (
     <button type="button" className="erp-theme-toggle" onClick={toggle} aria-pressed={theme === "dark"} aria-label={`Switch to ${theme === "dark" ? "light" : "dark"} theme`}}>
-      {theme === "dark" ? "Ã¢â€”Â Dark" : "Ã¢â€”â€˜ Light"}
+      {theme === "dark" ? "Dark" : "Light"}
     </button>
   );
 }
+
+export function CopyButton({
+  value,
+  label = "value"
+}: {
+  value: string;
+  label?: string;
+}) {
+  const [state, setState] = useState<"idle" | "copied" | "failed">("idle");
+
+  async function copy() {
+    try {
+      await navigator.clipboard.writeText(value);
+      setState("copied");
+    } catch {
+      setState("failed");
+    }
+    window.setTimeout(() => setState("idle"), 2000);
+  }
+
+  const accessibleLabel = state === "copied"
+    ? `${label} copied`
+    : state === "failed"
+      ? `Copy ${label} failed`
+      : `Copy ${label}`;
+
+  return (
+    <span className="erp-copy-control">
+      <button type="button" className="erp-icon-button" aria-label={accessibleLabel} title={`Copy ${label}`} onClick={copy}>
+        <LucideIcon name={state === "copied" ? "check" : "copy"} size={16} />
+      </button>
+      <span className="erp-sr-only" aria-live="polite">{state === "copied" ? `${label} copied` : state === "failed" ? `Copy ${label} failed` : ""}</span>
+    </span>
+  );
+}
*** End Patch
